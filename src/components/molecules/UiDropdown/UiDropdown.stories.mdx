import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiDropdown from './UiDropdown.vue';
import UiDropdownItem from './_internal/UiDropdownItem.vue';
import UiText from '../../atoms/UiText/UiText.vue';

<Meta
  title="Molecules/Dropdown"
  component={UiDropdown}
  argTypes={{
    modifiers: {
      control: {
        type: 'inline-check',
        options: {
          'ui-dropdown--compact': 'ui-dropdown--compact'
        },
      },
    },
  }}
/>

# Dropdown

Use `default` slot and `text` props for common scenario

<Canvas>
  <Story
    name="Common"
    height="80px"
    args={{
      text: 'Dropdown toggle',
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiDropdown, UiDropdownItem },
      data() {
        return {
          selected: {},
          symptoms: [
            { label: 'headache' },
            { label: 'head pressure' },
            { label: 'headache, generalized' },
            { label: 'headache, pulsating' },
          ],
        };
      },
      template: `<UiDropdown
        :class="modifiers"
        :class="modifiers"
        :text="text"
        v-model="selected"
        style="width: 100%; max-width: 304px;"
      >
      <template v-for="(symptom, key) in symptoms" :key="key">
        <UiDropdownItem :value="symptom">{{symptom.label}}</UiDropdownItem>
      </template>
      </UiDropdown>`,
    })).bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Common secondary"
    height="80px"
    args={{
      text: 'Dropdown toggle',
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiDropdown, UiDropdownItem, UiText },
      data() {
        return {
          selected: '',
          symptoms: [
            { label: 'headache' },
            { label: 'head pressure' },
            { label: 'headache, generalized' },
            { label: 'headache, pulsating' },
          ],
        };
      },
      template: `<UiDropdown
        :text="text"
      >
        <UiText>You’re about to use a short (3 min), safe and anonymous health checkup.</UiText>
      </UiDropdown>`,
    })).bind({})}
  </Story>
</Canvas>

# Props

<Props of={UiDropdown} />
