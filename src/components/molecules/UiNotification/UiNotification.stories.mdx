import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiNotification from './UiNotification.vue';
import UiLink from '../../atoms/UiLink/UiLink.vue';
import UiIcon from '../../atoms/UiIcon/UiIcon.vue';
import UiAlert from '../../atoms/UiAlert/UiAlert.vue';

<Meta
  title="Molecules/Notification"
  component={UiNotification}
  argTypes={{
    type: { control: { type: 'select', options: ['success', 'info', 'warning', 'error'] } },
  }}
/>

# Notification

<Canvas>
  <Story
    name="Common"
    args={{
      notification: 'Thank you. We’ll review this question as soon as possible',
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiNotification, UiAlert },
      template: `<UiNotification :type="type">
      <UiAlert
        :type="type"
        class="ui-alert--secondary">
          {{notification}}
      </UiAlert>
    </UiNotification>`,
    })).bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Notification with link"
    args={{
      type: 'error',
      notification: 'Thank you. We’ll review this question as soon as possible',
      href: 'https://infermedica.com/',
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: {
        UiNotification,
        UiAlert,
        UiLink,
        UiIcon,
      },
      template: `<UiNotification :type="type">
        <UiAlert :type="type" class="ui-alert--secondary">{{notification}}</UiAlert>
        <UiLink class="ui-link--has-icon ui-link--small" style="margin: var(--space-4) 0 0 var(--space-36); display: flex;">
          Skip this question <UiIcon icon="chevronRight" class="ui-link__icon ui-link__icon--right"/></UiLink>
      </UiNotification>`,
    })).bind({})}
  </Story>
</Canvas>

<ArgsTable story='Common' />

# Props

<Props of={UiNotification} />
