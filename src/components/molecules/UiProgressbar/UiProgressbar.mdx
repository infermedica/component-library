import { Canvas, Meta, Controls } from '@storybook/blocks';
import { Code } from '../../../../docs/components/Code';
import * as stories from './UiProgressbar.stories.js';
import scss from './UiProgressbar.stories.scss?raw';

<Meta of={stories}/>

# UiProgressbar
<Canvas of={stories.WithDots} />
<Controls of={stories.WithDots} />

## Stories

### Without Dots
<Canvas of={stories.WithoutDots}/>

**Use following SCSS to get the same result**
<Code
  id="progressbar-without-dots"
  code={ scss }
/>

### With Label
<Canvas of={stories.WithLabel}/>

### With Step Counter
<Canvas of={stories.WithStepCounter}/>

**Use following SCSS to get the same result**
<Code
  id="progressbar-without-step-counter"
  code={ scss }
/>

### With Label Slot
<Canvas of={stories.WithLabelSlot}/>

### With Progress Slot
<Canvas of={stories.WithProgressSlot}/>
