import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiBulletPoints from './UiBulletPoints.vue';
import UiBulletPointsItem from './_internal/UiBulletPointsItem.vue';
import UiText from '../../atoms/UiText/UiText.vue';

<Meta
  title="Molecules/BulletPoints"
  component={UiBulletPoints}
  argTypes={{
    tag: { control: { type: 'select', options: ['ul', 'ol'] } },
    type: { control: { type: 'select', options: [1, 'a', 'A', 'i', 'I'] } },
    icon: { control: { type: 'text' } },
    modifiers: {
      control: {
        type: 'inline-check',
        options: {
          'ui-bullet-points-item--primary': 'ui-bullet-points-item--primary',
          'ui-bullet-points-item--secondary': 'ui-bullet-points-item--secondary',
        },
      },
    },
  }}
/>

# BulletPoints

<Canvas>
  <Story
    name="Common"
    args={{
      tag: 'ul',
      type: 1,
      icon: 'bullet',
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiBulletPoints, UiBulletPointsItem, UiText },
      data() {
        return {
          items: [
            'Painful swallowing',
            'Stuffy nose',
            'Sneeze',
            'Muscle pain',
            'Runny nose',
          ],
        }
      },
      template: `<UiBulletPoints :tag="tag" :type="type">
        <template v-for="(item, key) in items" :key="key">
          <UiBulletPointsItem :icon="icon" :class="modifiers">
            <UiText tag="span">{{item}}</UiText>
          </UiBulletPointsItem>
        </template>
      </UiBulletPoints>`,
    })).bind({})}
  </Story>
</Canvas>

<ArgsTable story='Common' />

# Props

<Props of={UiBulletPoints} />
