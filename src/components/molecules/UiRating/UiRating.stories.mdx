import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiRating from './UiRating.vue';

<Meta
  title="Molecules/Rating"
  component={UiRating}
  argTypes={{
    max: { control: { type: 'text' } },
    disabled: { control: { type: 'boolean' } },
    modifiers: {
      control: {
        type: 'inline-check',
        options: {
          'ui-rating--is-disabled': 'ui-rating--is-disabled',
        },
      },
    },
  }}
/>

# Rating

<Canvas>
  <Story
    name="Common"
    args={{
      max: '5',
      disabled: false,
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiRating },
      data() {
        return {
          rate: '3',
        }
      },
      template: `<UiRating
        v-model="rate"
        :class="modifiers"
        :radio-attrs="{disabled: disabled}"
        :max="max"
      />`,
    })).bind({})}
  </Story>
</Canvas>

# Props

<Props of={UiRating} />
