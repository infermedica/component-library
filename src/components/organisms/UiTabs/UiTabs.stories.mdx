import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiTabs from './UiTabs.vue';
import UiTabsItem from './_internal/UiTabsItem.vue';
import UiText from '../../atoms/UiText/UiText.vue';

<Meta
  title="Organisms/Tabs"
  component={UiTabs}
/>

# Tabs

<Canvas>
  <Story
    name="Common"
  >
    {((args, { argTypes }) => ({
      components: {
        UiTabs,
        UiTabsItem,
        UiText,
      },
      data() {
        return {
          opened: 'search',
          items: [
            {
              title: 'Search',
              name: 'search',
              content: 'Serum uric acid concentration',
            },
            {
              title: 'Point on the body',
              name: 'point-on-the-body',
              content: '1. Erythrocyte Sedimentation Rate',
            },
          ],
        };
      },
      template: `<UiTabs v-model="opened">
        <template
          v-for="(item, key) in items"
          :key="key"
        >
          <UiTabsItem
            :name="item.name"
            :title="item.title"
          >
            <UiText>{{ item.content }}</UiText>
          </UiTabsItem>
        </template>
      </UiTabs>`,
    })).bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="[example] adjusted to design"
    parameters={{
      layout: 'full',
    }}
  >
    {((args, { argTypes }) => ({
      components: {
        UiTabs,
        UiTabsItem,
        UiText,
      },
      data() {
        return {
          opened: 'search',
          items: [
            {
              title: 'Search',
              name: 'search',
              content: 'Serum uric acid concentration',
            },
            {
              title: 'Point on the body',
              name: 'point-on-the-body',
              content: '1. Erythrocyte Sedimentation Rate',
            },
          ],
        };
      },
      template: `<UiTabs v-model="opened" style="--tabs-padding: 0 var(--space-20); --tabs-underline-x-default: var(--space-20); margin: -8px">
        <template
          v-for="(item, key) in items"
          :key="key"
        >
          <UiTabsItem
            :name="item.name"
            :title="item.title"
          >
            <UiText>{{ item.content }}</UiText>
          </UiTabsItem>
        </template>
      </UiTabs>`,
    })).bind()}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Fixed"
  >
    {((args, { argTypes }) => ({
      components: {
        UiTabs,
        UiTabsItem,
        UiText,
      },
      data() {
        return {
          opened: 'search',
          items: [
            {
              title: 'Search',
              name: 'search',
              content: 'Serum uric acid concentration',
            },
            {
              title: 'Point on the body',
              name: 'point-on-the-body',
              content: '1. Erythrocyte Sedimentation Rate',
            },
          ],
        };
      },
      template: `<UiTabs
        v-model="opened"
        class="ui-tabs--fixed"
      >
        <template
          v-for="(item, key) in items"
          :key="key"
        >
          <UiTabsItem
            :name="item.name"
            :title="item.title"
          >
            <UiText>{{ item.content }}</UiText>
          </UiTabsItem>
        </template>
      </UiTabs>`,
    })).bind()}
  </Story>
</Canvas>