import {
  Meta, Canvas, Story, Props, ArgsTable,
} from '@storybook/addon-docs/blocks';
import UiControls from './UiControls.vue';
import UiMultipleChoices from '../UiMultipleChoices/UiMultipleChoices.vue';
import UiTile from '../../molecules/UiTile/UiTile.vue';
import UiLink from '../../atoms/UiLink/UiLink.vue';

<Meta
  title="Organisms/Controls"
  component={UiControls}
  argTypes={{
    isAnswerAbove: { control: { type: 'boolean' } },
    invalid: { control: { type: 'boolean' } },
    toBack: { control: { type: 'text' } },
    toNext: { control: { type: 'text' } },
    translation: { control: { type: 'object' } },
  }}
/>

# Question

<Canvas>
  <Story
    name="Common"
    args={{
      toBack: '/3-3',
      toNext: '/5-4',
      invalid: true,
      translation: {
        back: 'Back',
        next: 'Next',
        answerAbove: 'Select an answer above',
      },
    }}
  >
    {((args, { argTypes }) => ({
      props: Object.keys(argTypes),
      components: { UiControls },
      template: `<UiControls
        :is-answer-above="isAnswerAbove"
        :invalid="invalid"
        :to-back="toBack"
        :to-next="toNext"
        :translation="translation"
        style="max-width: 780px; min-height: 465px"
      />`,
    })).bind({})}
  </Story>
</Canvas>

# Props

<Props of={UiControls} />
