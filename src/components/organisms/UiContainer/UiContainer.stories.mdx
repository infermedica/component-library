import { Meta, Canvas, Story, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import UiContainer from './UiContainer.vue';
import UiText from '../../atoms/UiText/UiText.vue';

<Meta
  title="Organisms/Container"
  component={UiContainer}
  argTypes={{
    content: {control: {type: 'text'}}
  }}
/>

# Container

<Canvas>
  <Story
    name="Common"
    args={{
      content: 'Your symptoms may require prompt medical evaluation. If your symptoms suddenly get worse, go to the nearest emergency department.'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiContainer, UiText},
      template: `<UiContainer>
        <UiText>{{content}}</UiText>
      </UiContainer>`
    })).bind({})}
  </Story>
</Canvas>

# Props

<Props of={UiContainer} />
