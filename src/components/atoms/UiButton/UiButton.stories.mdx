import { Meta, Canvas, Story, ArgsTable, } from '@storybook/addon-docs/blocks';
import UiButton from './UiButton.vue';
import UiIcon from '../UiIcon/UiIcon.vue';

<Meta
  title="Atoms/Button"
  component={UiButton}
  argTypes={{
    content: {control: {type: 'text'}},
    disabled: {control: {type: 'boolean'}},
    icon: {control: {type: 'text'}},
    modifiers: {control: {type: 'inline-check', options: {
      'ui-button--small': 'ui-button--small',
      'ui-button--is-disabled': 'ui-button--is-disabled',
    }}},
    tag: {control: {type: 'text'}},
    to: {control: {type: 'text'}},
    href: {control: {type: 'text'}}
  }}
/>

# Button
## Contained
<Canvas>
  <Story
    name="Contained"
    args={{
      content: 'Next',
      disabled: false,
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiButton},
      template: `
        <UiButton
          :class="modifiers"
          :disabled="disabled"
        >
          {{ content }}
        </UiButton>
      `
    })).bind({})}
  </Story>
</Canvas>

## Outlined
<Canvas>
  <Story
    name="Outlined"
    args={{
      content: 'Next',
      disabled: false,
      icon: 'reversePlus'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiButton, UiIcon},
      template: `
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--outlined"
            :class="modifiers"
            :disabled="disabled"
          >
            {{ content }}
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--outlined ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            <UiIcon
              :icon="icon"
              class="ui-button__icon"
            />
            <span>{{ content }}</span>
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--outlined ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            <span>{{ content }}</span>
            <UiIcon
              :icon="icon"
              class="ui-button__icon ui-button__icon--right"
            />
          </UiButton>
        </div>
      `
    })).bind({})}
  </Story>
</Canvas>

## Circled
<Canvas>
  <Story
    name="Circled"
    args={{
      disabled: false,
      icon: 'plus'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiButton, UiIcon},
      template: `
        <UiButton
          class="ui-button--outlined ui-button--circled ui-button--has-icon"
          :class="modifiers"
          :disabled="disabled"
        >
          <UiIcon
            :icon="icon"
          />
        </UiButton>
      `
    })).bind({})}
  </Story>
</Canvas>

## Text
<Canvas>
  <Story
    name="Text"
    args={{
      content: 'Next',
      disabled: false,
      icon: 'reversePlus'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiButton, UiIcon},
      template: `
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text"
            :class="modifiers"
            :disabled="disabled"
          >
            {{ content }}
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            {{ content }}
            <UiIcon
              :icon="icon"
              class="ui-button__icon ui-button__icon--right"
            />
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            <UiIcon
              :icon="icon"
              class="ui-button__icon"
            />
            {{ content }}
          </UiButton>
        </div>
      `
    })).bind({})}
  </Story>
</Canvas>

## Text secondary
<Canvas>
  <Story
    name="Text secondary"
    args={{
      content: 'Next',
      disabled: false,
      icon:'reversePlus',
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiButton, UiIcon},
      template: `
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text ui-button--secondary"
            :class="modifiers"
            :disabled="disabled"
          >
            {{ content }}
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text ui-button--secondary ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            {{ content }}
            <UiIcon
              :icon="icon"
              class="ui-button__icon ui-button__icon--right"
            />
          </UiButton>
        </div>
        <div style="margin-bottom: 20px;">
          <UiButton
            class="ui-button--text ui-button--secondary ui-button--has-icon"
            :class="modifiers"
            :disabled="disabled"
          >
            <UiIcon
              :icon="icon"
              class="ui-button__icon"
            />
            {{ content }}
          </UiButton>
        </div>
      `
    })).bind({})}
  </Story>
</Canvas>

## Button as link

<Canvas>
  <Story
    name="Button as link"
    args={{
      content: 'Next',
      tag: 'button',
      to: '',
      href: 'https://infermedica.com/'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: { UiButton },
      template: `<UiButton
      class="ui-button--text"
      :tag="tag"
      :to="to"
      :href="href"
      >
         {{ content }}
       </UiButton>`
    })).bind({})}
  </Story>
</Canvas>

# Props

<Props of={UiButton} />
