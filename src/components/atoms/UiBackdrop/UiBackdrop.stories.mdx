import { Meta, Canvas, Story, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import UiBackdrop from './UiBackdrop.vue';

<Meta
  title="Atoms/Backdrop"
  component={UiBackdrop}
  argTypes={{
    customProp: {control: {type: 'text'}}
  }}
/>

# Backdrop

<Canvas>
  <Story
    name="Common"
    args={{
      customProp: 'customProp'
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiBackdrop},
      template: `<UiBackdrop customProp="customProp"/>`
    })).bind({})}
  </Story>
</Canvas>

<ArgsTable story='Common' />

# Props

<Props of={UiBackdrop} />
