import { Meta, Canvas, Story, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import UiAlert from './UiAlert.vue';
import UiIcon from '../UiIcon/UiIcon.vue';
import UiLink from '../UiLink/UiLink.vue';
import UiText from '../UiText/UiText.vue';


<Meta
  title="Atoms/Alert"
  component={UiAlert}
  argTypes={{
    type: {control: {type: 'select', options: ['success', 'info', 'warning', 'error']}},
  }}
/>

# Alert

## Regular usage
<Canvas>
  <Story
    name="Common"
    args={{
      type: 'error',
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiAlert},
      template: `<UiAlert :type="type">Error message</UiAlert>`
    })).bind({})}
  </Story>
</Canvas>

## Custom alert icon
<Canvas>
  <Story
    name="[slot] icon"
    args={{
      type: 'error',
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiAlert, UiIcon},
      template: `<UiAlert :type="type">
        <template #icon>
          <UiIcon
            icon="reversePlus"
            class="ui-alert__icon"
          />
        </template>
        Custom icon
      </UiAlert>`
    })).bind({})}
  </Story>
</Canvas>

## Custom alert message
<Canvas>
  <Story
    name="[slot] message"
    args={{
      type: 'error',
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiAlert, UiLink, UiIcon, UiText},
      template: `<UiAlert :type="type">
        <template #message>
          <UiLink
            href="https://infermedica.com/"
            target="_blank"
          >
            <UiText
              class="ui-alert__message"
            >
              Custom message as link
            </UiText>
          </UiLink>
        </template>
      </UiAlert>`
    })).bind({})}
  </Story>
</Canvas>

## Custom alert message and icon
<Canvas>
  <Story
    name="[slot] icon [slot] message"
    args={{
      type: 'error',
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiAlert, UiLink, UiIcon, UiText},
      template: `<UiAlert :type="type">
        <template #icon>
          <UiIcon
            icon="reversePlus"
            class="ui-alert__icon"
          />
        </template>
        <template #message>
          <UiLink
            href="https://infermedica.com/"
            target="_blank"
          >
            <UiText
              class="ui-alert__message"
            >
              Custom message as link and custom icon
            </UiText>
          </UiLink>
        </template>
      </UiAlert>`
    })).bind({})}
  </Story>
</Canvas>

<ArgsTable story='Common' />

# Props

<Props of={UiAlert} />
