import { Meta, Canvas, Story, Props, ArgsTable } from '@storybook/addon-docs/blocks';
import UiHeading from './UiHeading.vue';

<Meta
  title="Atoms/Heading"
  component={UiHeading}
  argTypes={{
    level: {control: {type: 'select', options: ['1', '2', '3', '4', '5', '6']}},
    tag: {control: {type: 'text' }},
    cssClass: {control: {type: 'select', options: ['h1', 'h2', 'h3', 'h4', 'h5', 'h6']}},
  }}
/>

# Heading

This component works like the native heading. [MDN web docs](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Heading_Elements)

## Common heading

<Canvas>
  <Story
    name="Common"
    args={{
      level: '2',
      tag: '',
      cssClass: ''
    }}
  >
    {((args, {argTypes}) => ({
      props: Object.keys(argTypes),
      components: {UiHeading},
      template: `<UiHeading :level="level" :tag="tag" :class="cssClass">Symptom Checker</UiHeading>`
    })).bind({})}
  </Story>
</Canvas>


<ArgsTable story='Common' />

# Props

<Props of={UiHeading} />
