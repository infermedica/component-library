import { Meta, Canvas } from '@storybook/blocks';
import PageOutline from '../../components/PageOutline';
import * as stories from './css-logical-properties.stories.js';
import Code from '../../components/Code.jsx';
import Table from '../../components/Table.jsx';

<Meta of={stories}/>

# CSS Logical Properties
The Infermedica component library uses [CSS Logical Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Logical_Properties)
that provide the ability to control the layout through logical, not physical representations of directions and dimensions.
It give us a lot more power to control our websites with minimal style changes, no matter what type of language we use
English, Polish, Arabic, etc.

<PageOutline/>

## How to use
You can use the following `use-logical` mixin, which transforms conventional ltr CSS physical value to CSS logical properties.

<Code code={`@include mixins.use-logical(element, property, value);`}/>

for example

<Code
  code={`@infermedica/component-library/mixins;\n\n.ui-button {\n  @include mixins.use-logical("button", padding, var(--space-12) var(--space-32));\n}`}
/>

### Mixin arguments:
#### Property
- `margin`
- `padding`
- `border`
- `border-width`
- `border-style`
- `border-color`
- `border-radius`
- `inset`

Here is an example of logical properties and CSS variables for - `margin`, `padding`, `border`, `inset`.
<Table
  headers={['Physical property', 'Logical property', 'CSS variable']}
  rows={[
    ['`{property}-top`', '`{property}-block-start`', '`--{element}-{property}-block-start`'],
    ['`{property}-right`', '`{property}-inline-end`', '`--{element}-{property}-inline-end`'],
    ['`{property}-bottom`', '`{property}-block-end`', '`--{element}-{property}-block-end`'],
    ['`{property}-left`', '`{property}-inline-start`', '`--{element}-{property}-inline-start`'],
    ['`{property}-top` `{property}-bottom`', '`{property}-block`', '`--{element}-{property}-block`'],
    ['`{property}-left` `{property}-right`', '`{property}-inline`', '`--{element}-{property}-inline`'],
    ['`{property}`', '`{property}-block` `{property}-inline`', '`--{element}-{property}-block` `--{element}-{property}-inline`'],
  ]}
/>

Here is an example of logical properties and CSS variables for `border-width`, `border-style` and `border-color`.
<Table
  headers={['Physical property', 'Logical property', 'CSS Variable']}
  rows={[
    [
      '`border-top-{width/style/color}`',
      '`border-block-start-{width/style/color}`',
      '`--{element}-border-block-start-{width/style/color}`'
    ],[
      '`border-right-{width/style/color}`',
      '`border-inline-end-{width/style/color}`',
      '`--{element}-border-inline-end-{width/style/color}`'
    ],[
      '`border-bottom-{width/style/color}`',
      '`border-block-end-{width/style/color}`',
      '`--{element}-border-block-end-{width/style/color}`'
    ],[
      '`border-left-{width/style/color}`',
      '`border-inline-start-{width/style/color}`',
      '`--{element}-border-inline-start-{width/style/color}`'
    ],[
      '`border-{width/style/color}`',
      '`border-block-{width/style/color}` `border-inline-{width/style/color}`',
      '`--{element}-border-block-{width/style/color}` `--{element}-border-inline-{width/style/color}`'],
  ]}
/>

Here is an example of logical properties and CSS variables for `border-radius`.
<Table
  headers={['Physical property', 'Logical property', 'CSS variable']}
  rows={[
    [
      '`border-top-left-radius`',
      '`border-start-start-radius`',
      '`--{element}-border-start-start-radius`'
    ],[
      '`border-top-right-radius`',
      '`border-start-end-radius`',
      '`--{element}-border-start-end-radius`'
    ],[
      '`border-bottom-left-radius`',
      '`border-end-start-radius`',
      '`--{element}-border-end-start-radius`'
    ],[
      '`border-bottom-right-radius`',
      '`border-end-end-radius`',
      '`--{element}-border-end-end-radius`'
    ],
  ]}
/>

#### Value
Please use conventional ltr CSS value for property (top right bottom left) e.g. `20px 30px 10px 40px`:
<Code code={`
{property}-block: 20px 10px;
{property}-inline: 30px 40px;
`}/>

- When a value has three declaration, it is a shortcut to set `inline-start` property e.g `20px 30px 10px`

<Code code={`
{property}-block: 20px 10px;
{property}-inline: 30px 30px;
`}/>

- When a value has two declaration, it is a shortcut to set block and inline properties e.g `20px 30px`

<Code code={`
{property}-block: 20px 20px;
{property}-inline: 30px 30px;
`}/>

- When a value has one declaration, it is a shortcut to set all properties e.g. `20px`

<Code code={`
{property}-block: 20px 20px;
{property}-inline: 20px 20px;
`}/>

## Easy way for rtl support
CSS Logical Properties respond automatically to text direction and typing mode. This means that the left and right sides will be automatically reversed and
you won't need to add styles for rtl layouts anymore.

<Code code={`.ui-list {
  // deprecated
  margin: 0 var(--space-4) 0 var(--space-8);'\n
  [dir="rtl"] & {
    margin: 0 var(--space-8) 0 var(--space-4);
  }
  // Please, use this instead
  @include mixins.use-logical(list, margin, 0 var(--space-4) 0 var(--space-8));
}
`}/>

## CSS Logical Properties and Values
### Padding

<Canvas of={stories.Padding}/>

### Margin
<Canvas of={stories.Margin}/>

### Border width
<Canvas of={stories.BorderWidth}/>

### Border radius
<Canvas of={stories.BorderRadius}/>
