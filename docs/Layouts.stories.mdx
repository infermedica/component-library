import { Meta, Story } from '@storybook/addon-docs/blocks';
import DocsTable from './components/DocsTable.vue';

<Meta
  title="Docs/Design/Layouts"
/>

# Space

<Story name="Space" height="660px">
  {{
    components: {
      DocsTable
    },
    data() {
      return {
        px: false,
        spaces: [
          {
            size: {rem: '0.125rem', px: '2px'},
            token: '--space-2',
            description: 'Default non-interactive body text color.',
          },
          {
            size: {rem: '0.25rem', px: '4px'},
            token: '--space-4',
            description: '',
          },
          {
            size: {rem: '0.5rem', px: '8px'},
            token: '--space-8',
            description: '',
          },
          {
            size: {rem: '0.75rem', px: '12px'},
            token: '--space-12',
            description: '',
          },
          {
            size: {rem: '1rem', px: '16px'},
            token: '--space-16',
            description: '',
          },
          {
            size: {rem: '1.25rem', px: '20px'},
            token: '--space-20',
            description: '',
          },
          {
            size: {rem: '1.5rem', px: '24px'},
            token: '--space-24',
            description: '',
          },
          {
            size: {rem: '2rem', px: '32px'},
            token: '--space-32',
            description: '',
          },
          {
            size: {rem: '2.5rem', px: '40px'},
            token: '--space-40',
            description: '',
          },
          {
            size: {rem: '3rem', px: '48px'},
            token: '--space-48',
            description: '',
          },
          {
            size: {rem: '4rem', px: '64px'},
            token: '--space-64',
            description: '',
          }
        ]
      }
    },
    computed: {
      unit(){
        return this.px ? 'px' : 'rem'
      }
    },
    template: `<div>
      <label style="display: flex; align-items: center; margin: 1rem 0; display: block;"><input v-model="px" type="checkbox">show in px</label>
      <DocsTable>
        <template #thead>
          <th>Preview</th>
          <th>Value</th>
          <th>Token</th>
          <th>Usage</th>
        </template>
        <template #tbody>
          <template v-for="space in spaces">
            <tr>
              <td>
                <div :style="'width: var(' + space.token + '); height: var(' + space.token + '); background: #C2E0F8; border-radius: 5px'"></div>
              </td>
              <td>
                {{ space.size[unit] }}
              </td>
              <td>{{ space.token }}</td>
              <td>{{ space.description }}</td>
            </tr>
          </template>
        </template>
      </DocsTable>
    </div>`
  }}
</Story>

# Border radius

<Story name="Border radius" height="380px">
  {{
    components: {
      DocsTable
    },
    data() {
      return {
        radius: [
          {
            size: [120, 24],
            value: '0',
            token: '--border-radius-sharp',
            description: 'Non-rounded elements.',
          },
          {
            size: [120, 24],
            value: '5px',
            token: '--border-radius-default',
            description: 'Default border radius for cards, tiles, modals, dropdowns.',
          },
          {
            size: [120, 24],
            value: '5px',
            token: '--border-radius-form',
            description: 'Default border radius for form elements.',
          },
          {
            size: [120, 24],
            value: '20rem',
            token: '--border-radius-pill',
            description: 'Border radius for fully rounded rectangular elements, like chips.',
          },
          {
            size: [24, 24],
            value: '50%',
            token: '--border-radius-circle',
            description: 'Border radius for circular elements (with equal width and height)',
          }
        ]
      }
    },
    template: `<DocsTable>
      <template #thead>
        <th>Preview</th>
        <th>Value</th>
        <th>Token</th>
        <th>Usage</th>
      </template>
      <template #tbody>
        <template v-for="border in radius">
          <tr>
            <td>
              <div :style="'background: #7D8FA3; width: ' + border.size[0] + 'px; height: ' + border.size[1] + 'px; border-radius: var(' + border.token + ')'"></div>
            </td>
            <td>
              {{ border.value }}
            </td>
            <td>{{ border.token }}</td>
            <td>{{ border.description }}</td>
          </tr>
        </template>
      </template>
    </DocsTable>`
  }}
</Story>

# Box shadows

<Story name="Box shadows" height="270px">
  {{
    components: {
      DocsTable
    },
    data() {
      return {
        shadows: [
          {
            value: '0px 2px 6px 0px rgba(125,143,163,0.2)',
            token: '--box-shadow-default',
            description: 'Use for cards',
          },
          {
            value: '',
            token: '',
            description: 'Use for sheets and modals with dark dropdown',
          },
        ]
      }
    },
    template: `<DocsTable>
      <template #thead>
        <th>Preview</th>
        <th>Value</th>
        <th>Token</th>
        <th>Usage</th>
      </template>
      <template #tbody>
        <template v-for="shadow in shadows">
          <tr>
            <td>
              <div :style="'background: #FFF; width: 64px; height: 64px; border-radius: 5px; box-shadow: var(' + shadow.token + ')'"></div>
            </td>
            <td>
              {{ shadow.value }}
            </td>
            <td>{{ shadow.token }}</td>
            <td>{{ shadow.description }}</td>
          </tr>
        </template>
      </template>
    </DocsTable>`
  }}
</Story>
