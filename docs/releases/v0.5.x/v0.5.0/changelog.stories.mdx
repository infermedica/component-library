import { Meta } from '@storybook/addon-docs';

<Meta title="Releases/v0.5.x/v0.5.0/Changelog" />

# v0.5.0 (June , 2022)

## ⛔️ Breaking Change
* Handle focus visibility in Infermedica Component Library. Rename class on `body` to `focus-hidden`.
Add helper `useFocus` to allow to focus element and force removing `focus-hidden`.
* Add action button as a native feature to [UiNotification](/docs/molecules-notification--error) component. Default slot place text inside alert.
To enable action use buttonActionAttrs and pass onClick on other listener.

# 🚀 Features
* New focus visible approach
* Add action button to [UiNotification](/docs/molecules-notification--error) component.
