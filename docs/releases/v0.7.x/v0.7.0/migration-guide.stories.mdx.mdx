import { Meta } from "@storybook/blocks";

<Meta title="Releases/v0.7.x/v0.7.0/Migration Guide"/>

# Migration Guide

## Move mobile UiStepper layout to UiProgressbar.
From v0.7.0, UiStepper will be displayed in the same way on every device. To get UiStepper mobile layout from previous versions you should use UiProgressbar with `label` property.
