import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import DocsColor from './components/DocsColor.vue'

<Meta
  title="Docs/Global variables/Color"
/>

# Color palette

## Gray

<Story
  name="gray"
>
  {(() => ({
    components: { DocsColor },
    template: `<DocsColor
    name="gray"
    :colors="[
      {props: 100, initial: '#f5f5f5'},
      {props: 200, initial: '#eeeeee'},
      {props: 300, initial: '#e0e0e0'},
      {props: 400, initial: '#bdbdbd'},
      {props: 500, initial: '#9e9e9e'},
      {props: 600, initial: '#757575'},
      {props: 700, initial: '#616161'},
      {props: 800, initial: '#424242'},
      {props: 900, initial: '#212121'},
    ]"/>`,
  })).bind({})}
</Story>

## Green
<Story
  name="green"
>
  {(() => ({
    components: { DocsColor },
    template: `<DocsColor
    name="green"
    :colors="[
      {props: 100, initial: '#F0FFF4'},
      {props: 200, initial: '#C6F6D5'},
      {props: 300, initial: '#9AE6B4'},
      {props: 400, initial: '#68D391'},
      {props: 500, initial: '#48BB78'},
      {props: 600, initial: '#38A169'},
      {props: 700, initial: '#2F855A'},
      {props: 800, initial: '#276749'},
      {props: 900, initial: '#22543D'},
    ]"/>`,
  })).bind({})}
</Story>

## Blue
<Story
  name="blue"
>
  {(() => ({
    components: { DocsColor },
    template: `<DocsColor
    name="blue"
    :colors="[
      {props: 100, initial: '#EBF8FF'},
      {props: 200, initial: '#BEE3F8'},
      {props: 300, initial: '#90CDF4'},
      {props: 400, initial: '#63B3ED'},
      {props: 500, initial: '#4299E1'},
      {props: 600, initial: '#3182CE'},
      {props: 700, initial: '#2B6CB0'},
      {props: 800, initial: '#2C5282'},
      {props: 900, initial: '#2A4365'},
    ]"/>`,
  })).bind({})}
</Story>
