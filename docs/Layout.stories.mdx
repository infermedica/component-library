import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import DocsTable from './components/DocsTable.vue';

<Meta
  title="Docs/Global variables/Layout"
/>

# Layout

## spacing

<Story
  name="spacing"
>
  {(() => ({
    components: { DocsTable },
    data() {
      return {
        spacings: [
          {props: 1, default: 0.25},
          {props: 2, default: 0.5},
          {props: 3, default: 0.75},
          {props: 4, default: 1},
          {props: 5, default: 1.25},
          {props: 6, default: 1.5},
          {props: 8, default: 2},
          {props: 10, default: 2.5},
          {props: 12, default: 3},
          {props: 16, default: 4},
          {props: 20, default: 5},
          {props: 24, default: 6},
          {props: 32, default: 8},
          {props: 40, default: 10},
        ]
      }
    },
    template: `<DocsTable style="margin: 0 -15px">
      <template #thead>
        <tr>
          <th>Render</th>
          <th>Property</th>
          <th>Default</th>
        </tr>
      </template>
      <template #tbody>
        <template v-for="(spacing, key) in spacings">
          <tr :key="key">
            <td><div :style="{height: '1rem', width: 'var(--s-'+spacing.props+')', background:'#e2e8f0'}"></div></td>
            <td>--s-{{spacing.props}}</td>
            <td>{{spacing.default}}rem</td>
          </tr>
        </template>
      </template>
    </DocsTable>`,
  })).bind({})}
</Story>
